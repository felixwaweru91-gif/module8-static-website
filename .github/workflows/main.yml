name: Website Monitor

on:
  push:
  schedule:
    - cron: '*/1 * * * *'   # runs every 1 minute

jobs:
  check-website:
    runs-on: ubuntu-latest

    steps:
      - name: Check website status
        run: |
          curl -f https://wrong-url-test-123.com || exit 1
